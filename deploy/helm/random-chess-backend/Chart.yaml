apiVersion: v2
name: random-chess-backend
description: Random Chess Backend
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: postgresql
    version: "~16.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
